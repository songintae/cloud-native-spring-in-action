apiVersion: v1
kind: Service

metadata:
  name: config-service
  labels:
    app: config-service

spec:
  type: NodePort
  selector:
    app: config-service
  ports:
    - port: 8888
      targetPort: 8888
      nodePort: 30002
      protocol: TCP