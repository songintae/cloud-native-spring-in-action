apiVersion: v1
kind: Service

metadata:
  name: catalog-service
  labels:
    app: catalog-service

spec:
  type: NodePort
  selector:
    app: catalog-service
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30001
      protocol: TCP